#include <cstdio>

// offline_render: render a project to a sequence of PNGs or an mp4 without
// opening a visible window. Requires a headless GL context (EGL on Linux);
// hookup happens once the renderer can run without a GLFW window.

int main(int /*argc*/, char** /*argv*/) {
    std::printf("offline_render: not yet implemented\n");
    return 0;
}
